.env
.env.local
.env.*.local
.DS_Store
.deepeval/
.claude/settings.local.json

coms/
# agent-service-toolkit/ — intentionally UNIGNORED: it is the PF-001 build base.
#   Its nested .git was moved to ~/.pf-001-backups/ so all files surface in THIS repo.
#   Restore it later (see that backup's RESTORE note) to push a branch/PR upstream.
alex/
books/
cyber/
Guardrails/
mem0/
open-webui/
production/
ragflow/
skills/
deepeval/
guardrails/
# graphify code-graph artifacts (local DX only — keep out of public repo)
graphify-out/
.graphify/

# Raw MITRE ATT&CK STIX cache (~35MB) — regenerated by scripts/fetch_attack_corpus.py.
# The distilled data/attack/attack_corpus.jsonl + .seed.jsonl ARE tracked.
data/attack/enterprise-attack.json

# Chroma dense index for the ATT&CK corpus — rebuilt by scripts/index_attack_corpus.py
# (binary + large; lives under the otherwise-unignored agent-service-toolkit build base).
agent-service-toolkit/chroma_db/
.humanlayer/workspace.local.json

# Riptide artifacts (cloud-synced)
.humanlayer/tasks/
