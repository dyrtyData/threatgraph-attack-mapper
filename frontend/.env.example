# Base URL of the FastAPI agent service (dev default port is 8081).
VITE_AGENT_URL=http://localhost:8081

# Optional bearer token — only needed if the service sets AUTH_SECRET.
# Leave unset for local runs where AUTH_SECRET is not configured.
# VITE_AGENT_TOKEN=
